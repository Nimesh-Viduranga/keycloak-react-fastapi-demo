# Keycloak / OIDC (baked into the Vite bundle at build time — public values only)
# Restart `npm run dev` after changing these.

VITE_KEYCLOAK_AUTHORITY=http://localhost:8090/realms/brandvisual
VITE_CLIENT_ID=react-fastapi-demo
